<h2>Student Form</h2>

<%= form_for(@student) do |f| %>
  <%= f.label :first_name %>:
  <%= f.text_field :first_name, :id => "student_first_name" %><br />

  <%= f.label :last_name %>:
  <%= f.text_field :last_name, :id => "student_last_name"%><br />

  <%= f.submit "Create Student"%>
<% end %>
